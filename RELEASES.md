# Releases

Append-only log of every release shipped to R2 by this repo. Read this before shipping — it's the source of truth for "what's actually live."

Format: `- YYYY-MM-DD — vX.Y.Z — short summary`. Most recent first within each app.

---

## beam-profiler

_(no successful R2 publish yet — v1.2.0 was tagged but the CI build failed on a Tauri dialog plugin version mismatch; v1.2.1 was rebuilt locally and is ready to ship as the first run of the new AI-driven flow)_
